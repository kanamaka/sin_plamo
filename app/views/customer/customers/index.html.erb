<header>
 <%= render 'layouts/header' %>
</header>
 <div class='container'>
   <h1>作品ランキング</h1><br>
   <h4>いいねの多い作品トップ3を紹介‼</h4>
   <table>
    <thead width="1200">
     <tr style="border:none;">
       <th width="300">作品画像</th>
       <th width="300">タイトル</th>
       <th width="300">スキ！の多さ</th>
        <th width="300">お名前</th>
      </td>
     </tr>
    </thead>
    <tr>
     <% @opu_ranks.each do |opus| %>
     <td>
      <%= link_to opu_path(opus.id) do %>
      <%= attachment_image_tag(opus.opus_images[0], :image, class: "sizee") %>
     <td><%= opus.opus_name %></td>
     <td><%= opus.favorites.count %></td>
     <td><%= @customer.name %></td>
     <% end %>
     </td>
    </tr>
    </p>
   <% end %>
   </table>
 </div>
<footer>
 <%= render 'layouts/footer' %>
</footer>